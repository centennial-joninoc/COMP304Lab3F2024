<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:enableOnBackInvokedCallback="true"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:usesCleartextTraffic="true"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Johninocente_COMP304Lab3_Ex1">
<!--        tools:targetApi="31">-->
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:theme="@style/Theme.Johninocente_COMP304Lab3_Ex1">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

    </application>

</manifest>

    <!--        Added this line in the manifest:   -->
    <!--            android:usesCleartextTraffic="true" -->
    <!--        due to the error:   -->
    <!--        FATAL EXCEPTION: main-->
    <!--        Process: com.example.johninocente_comp304lab3_ex1, PID: **** -->
    <!--        java.net.UnknownServiceException: CLEARTEXT communication to gd.geobytes.com not permitted by network security policy -->